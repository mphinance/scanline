fastapi>=0.110
uvicorn[standard]>=0.27
tradingview-screener>=3.0
pandas>=2.0
requests>=2.31
fastmcp>=2.0

# Test suite. CLAUDE.md documents `python -m pytest tests/ -q -m "not live"` as a
# standard command, so it belongs here: without it that command fails on a fresh
# install.
pytest>=8.0
